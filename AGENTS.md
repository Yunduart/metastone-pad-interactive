# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Locked interaction direction

- This is a standalone Xiaomi Pad 8 Pro 3:2 landscape video-on-demand program. It is not part of the ROMS CPU / GPU playback system and must not import, control, or modify that program.
- The supplied 3840 × 2160 screenshot is the visual source of truth for the star field, globes, satellite line art, central METASTONE identity, Chinese / English labels, and node locations.
- Globe art must read as luminous blue glass rather than a flat pale sphere: deep cobalt oceans, icy blue-white land and cloud detail, a soft atmospheric halo, and a restrained triangular network. Avoid dense latitude / longitude grids or hard concentric glow bands.
- Keep globe bodies one visual step darker than the surrounding labels and energy accents. Preserve readable continent relief, specular highlights, and atmospheric rims instead of turning the planets into black silhouettes.
- Blank-area drag rotates the complete seven-node galaxy with light inertia. Preserve DOM label alignment, safe-area clamping, click suppression after dragging, and reduced-motion behavior when refining the WebGL art.
- 2026-08-25 user feedback supersedes the blank-area-only gesture detail: the whole galaxy must feel directly draggable and spatial. A gesture may start on a planet or its label; use a movement threshold to arbitrate tap-to-play versus drag-to-orbit so node surfaces do not become dead drag zones or accidental playback triggers.
- Do not mix vertical delta into the same one-dimensional azimuth formula. Horizontal drag should control azimuth; vertical drag should control a restrained elevation/track tilt with explicit limits. Diagonals must feel isotropic rather than becoming faster or slower depending on whether the finger moves up or down.
- Tune inertia from a short recent-velocity window so slow, medium, and fast gestures remain distinguishable. Avoid early velocity saturation, edge compression/pile-up, zoom pumping, or a selected planet remaining clipped at the edge after release.
- Orbit motion must never hard-freeze at a screen edge. Use a continuous soft boundary, depth-aware planet motion, and drag-velocity-driven track tilt / view dolly so left-right gestures read as one spatial camera move rather than a flat slide.
- 2026-08-25 latest interaction decision: do not let the left side collapse into a standing vertical list. The central METASTONE / 是石科技 core is part of the world transform, not a fixed overlay; during horizontal drag it should visibly travel from left toward right while planets, orbit tracks, labels, and the selected energy link rotate as one spatial galaxy.
- 2026-08-25 inverse-parallax direction decision: keep the METASTONE / 是石科技 core at the left-side default (source X=34%). A leftward finger drag must move that core toward the right, while a rightward drag moves it back toward the left; the orbit azimuth still follows the gesture so the core and rotating galaxy create a clear opposing camera-parallax cue.
- 2026-08-25 two-axis rebound decision: extend the same inverse-parallax cue vertically (upward finger drag moves the galaxy down; downward drag moves it up). On pointer release or cancellation, horizontal/vertical translation, orbit azimuth, and elevation must ease back to the original effect-image composition; reduced-motion users return immediately.
- 2026-08-25 latest composition decision: preserve the supplied effect image's asymmetric composition (left-mid METASTONE core, deliberately uneven planet spacing, and varied planet scale). Do not redistribute the seven planets evenly. Map the original portal positions onto the visible primary/outer rings, preserve their non-uniform angular gaps, and rotate the whole composition with those rings throughout drag, elevation, inertia, and catalog switching.
- 2026-08-27 art and interaction refinement: preserve that locked composition while adding restrained near/far star parallax, depth-weighted planet/label brightness, a selected-node orbital halo, tactile feedback at the drag threshold on supported touch devices, and a damped spring return to the default composition. These cues must improve spatial depth without introducing zoom pumping, dense effects, or motion that survives `prefers-reduced-motion`.
- Orbit tracks should read as three-dimensional paths with distinct front/back values and remain visually coherent with the drag direction. Keep Chinese planet titles slightly smaller than the icon-led hierarchy; icons should carry more visual weight.
- The galaxy background should be predominantly deep black-indigo rather than royal blue. Retain only restrained cold-blue nebula haze and crisp star highlights so the luminous planets and orbital paths remain the visual lead.
- The seven selectable video themes are: 大模型, 科研院所, 高端制造, 海洋模拟, 航空航天, 互联网, and AI FOR SCIENCE. The central METASTONE node returns to overview.
- One shared WebGL runtime owns two top-level content catalogs: `成果案例 / SUCCESS CASES` and `产品介绍 / PRODUCT INTRODUCTION`. Do not fork these into duplicate applications; catalog switching must retain the same galaxy, touch model, player anatomy, and central METASTONE reset behavior.
- `成果案例` contains the seven existing industry themes.
- 2026-08-25 product-label decision: `产品介绍` now exposes PRODUCT-01—09 as nine named product nodes backed by 11 source-film records. PRODUCT-03 owns three films; the other products own one each. Keep the seven-node asymmetric composition unchanged for `成果案例`, while the product catalog uses its own non-uniform nine-node main/outer-ring layout and the same drag/projection behavior.
- The selected energy link carries a small spacecraft icon that follows the curve tangent. Do not revert it to an abstract pulse sphere.
- Every content node opens the same full-screen Pad control anatomy with back, previous item, play / pause, next item, read-only progress, rate, mute, and exit controls. Keep playlist media mapping in `src/domains.js` and the replacement contract in `public/videos/README.md`.
- The functional test topology is two-device: Xiaomi Pad is the control-only surface and a separate TV/browser is the sole playback display. Both connect to the same LAN control server; Pad actions for select/play, pause/resume, rate, mute, next/previous item, and exit must update the TV state. The Pad must not mount an HTML video element, download the selected MP4, decode media, or claim playback authority.
- 2026-09-03 Pad playback-control decision supersedes the earlier Pad heartbeat wording: the Pad timeline is read-only and must not support scrubbing. Expose play/pause and the approved rates `1× / 2× / 4×` (with the Chinese UI label `原速` for `1×`), while retaining playlist navigation, mute, and exit. The LAN controller owns the authoritative progress anchor, playback ID, item index, duration, loop state, and client command sequence, and streams state to Pad/TV every 500ms; only the TV/browser corrects playback drift and applies the selected rate. Keep legacy `SEEK`/`SYNC` server handling only for protocol compatibility, not as Pad behavior.
- 2026-09-03 latest media decision: map the real files from `D:\FOR_WORK\260818_MetaStone\0824案例介绍` and `D:\FOR_WORK\260818_MetaStone\0817是石科技产品视频互动展示` as mixed-media playlists. Convert every PPT page into its own temporary looping H.264 MP4 at 1920×1080; preserve embedded GIF animation (航空航天 and 高端制造) rather than flattening it. These stable item IDs/paths are replacement slots for later final motion videos.
- 2026-09-03 Pad control visual decision: translate the approved music-player reference into the existing cosmic system without copying its portrait artwork. Use one centered orbital control cluster with a luminous circular play / pause core, previous and larger Chinese `下一项` controls around it, a read-only synchronized light track, and visually subordinate `原速 / 2× / 4×` plus mute controls. Keep the Pad controller-only and preserve the exact media directory.
- 2026-09-03 visual rejection supersedes the heavy first pass: do not use a large half-dome HUD, stacked neon rings, or three disconnected bottom cards. Keep the same control behavior, but present it as one restrained floating transport dock with a single subtle play halo, quiet separators, and a clearly larger—yet not toy-like—`下一项` action.
- 2026-09-03 glass-material clarification: the selected effect reference depends on translucent glass, not flat navy fills. Use background-visible blur, modest saturation lift, a fine cold edge, and one restrained inner highlight on the playlist and transport dock; never turn “glass” back into opaque blue cards, thick glow stacks, or decorative chrome.
- 2026-09-04 control-scale correction supersedes the intentionally mismatched previous/next sizes: the footer was too small and mixed 32/46/56/68 px controls. Use one 64–80 px responsive control-height token for return, previous, play/pause, next, every playback rate, and mute; previous/next share the same width and typography. Give playback priority through its white glass core, not an oversized footprint. Keep every action on the same vertical centerline and move the read-only progress to its own row above them; enlarge the dock and labels without changing its glass material or controller-only behavior.
- The TV route begins on a branded standby screen. On the first Pad play command, the standby surface fades out while the selected video fades in; stopping or leaving playback returns the TV to standby. Preserve the one-time TV-side playback/fullscreen activation required by browser autoplay policies.
- 2026-08-31 TV hardware and standby decision: retain the existing REDMI televisions as display-only endpoints. Add two independent playback boxes / mini PCs, one per television, with locked Pad/controller/channel/target-endpoint bindings; the exact REDMI model and playback-box SKU remain pending site inspection, quotation, and sample verification.
- 2026-08-31 standby art decision: the `/tv` standby uses the existing asymmetric galaxy world rather than a separate visual language. Preserve the left-mid METASTONE core, uneven seven-planet spacing, deep black-indigo field, subtle depth parallax, and a 12-second seamless low-motion loop. Visitor-facing copy is `成果案例 · 产品介绍 / 请在 Pad 端选择内容`; connection and first-run activation controls remain visually subordinate.
- The playback box procurement line includes two boxes, HDMI 2.0+ cables, Gigabit Ethernet patch cables, hidden mounting hardware, and power/cable-management kits; one same-model spare is an optional separately priced line. Treat all as pending selection, quotation, and sample validation rather than an approved purchase order.
- The test server must serve `/videos/` directly from `public/videos/` with byte-range support so newly dropped MP4 files can be tested without rebuilding. Missing files fall back to a clearly labeled demo slot and must not break Pad/TV control flow.
